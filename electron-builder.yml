appId: io.todaylist.today
productName: Today
artifactName: Today-${version}.${ext}
asar: true
afterSign: scripts/afterSign.js
afterPack: scripts/afterPack.js

directories:
  output: dist

files:
  - temp/dist/**/*
  # - embedded.provisionprofile

mac:
  icon: temp/build/icons/mac/icon.icns
  category: public.app-category.productivity
  hardenedRuntime: true
  entitlements: temp/build/entitlements.mac.plist
  # entitlementsInherit: build/entitlements.mac.plist
  # provisioningProfile: embedded.provisionprofile
  gatekeeperAssess: false
  target:
    - dmg
    - pkg
    - zip

dmg:
  icon: temp/build/icons/mac/icon.icns
  iconSize: 128
  contents:
    - { x: 130, y: 220 }
    - { x: 410, y: 220, type: link, path: /Applications }
win:
  icon: temp/build/icons/windows/icon.ico
  target:
    - target: zip
    - target: nsis
      arch:
        - ia32

nsis:
  oneClick: false
  perMachine: true
  createDesktopShortcut: true
  createStartMenuShortcut: true
  allowToChangeInstallationDirectory: true
  installerIcon: temp/build/icons/windows/icon.ico
  shortcutName: Today
  artifactName: Today-${version}.${ext}

linux:
  category: Utility
  icon: temp/build/icons/png
  target:
    - target: AppImage
    - target: deb
    - target: tar.gz
# releaseInfo:
#   releaseNotes:
